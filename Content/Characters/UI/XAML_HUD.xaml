<UserControl x:Class="hud.ParentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:HUD="clr-namespace:hud"
    HorizontalAlignment="Stretch" 
    VerticalAlignment="Stretch">

    <!-- Start Screen -->
    <Grid x:Name="StartScreen">

        <hud:Radar x:Name="Radar" />
            <!--
                Visibility="{Binding bIsRadarVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
            -->
        

        <!-- Reticle! -->
        <Ellipse Width="10" Height="10" Stroke="White" Fill="Black" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Button Content="Hello button" Click="Button_Click"/>

        <!-- Placeholder for Arsenal -->
        <!-- Abilities -->
        <!-- Weapons -->

    </Grid>
</UserControl>
